# Development / CI dependencies (not needed to run the app).
# Install with:  pip install -r requirements.txt -r requirements-dev.txt
-r requirements.txt

pytest>=8.0.0        # test runner (also listed in requirements.txt for convenience)
ruff>=0.5.0          # linter + formatter
mypy>=1.10.0         # static type checker
httpx>=0.27.0        # Starlette TestClient dependency for the API tests
